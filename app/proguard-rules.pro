# Add project specific ProGuard rules here.
# This training app ships with minification disabled by design —
# see docs/VULNERABILITIES.md (Reverse Engineering Exposure).
-keepattributes SourceFile,LineNumberTable
